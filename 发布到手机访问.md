# 发布到 GitHub Pages

当前目录 `diary-site` 已经是可部署网页。

## 网页端最快步骤

1. 打开 GitHub，新建一个仓库，比如 `private-diary`。
2. 上传本目录里的所有文件：
   - `index.html`
   - `manifest.webmanifest`
   - `sw.js`
   - `icon.png`
   - `icon-512.png`
   - `README.md`
3. 进入仓库 `Settings -> Pages`。
4. Source 选择 `Deploy from a branch`。
5. Branch 选择 `main`，目录选择 `/root`。
6. 等待 GitHub Pages 生成网址。

## 手机上使用

1. 用手机浏览器打开 GitHub Pages 网址。
2. 浏览器菜单里选择“添加到主屏幕”。
3. 以后可以像 App 一样打开。

## 重要提醒

- 网页程序会发布到网上，但你的日记内容不会自动上传。
- 日记文字保存在当前浏览器 `localStorage`。
- 附件保存在当前浏览器 `IndexedDB`。
- 电脑和手机不同步；换设备请用 JSON 备份导出/导入。
